/** Shared company-scoped admin panels (users, sites, departments, company info).
 *  Implemented via AdminWorkspace in company or platform mode. */
export { default } from './AdminWorkspace';
export type { AdminWorkspaceMode } from './AdminWorkspace';
